export struct USingleColor {
	light: brush,
	dark: brush,
}

export struct UThemeColor {
	base: brush,
	hover: brush,
	active: brush,
	disabled: brush,
	selected: brush,
}


export struct UDualThemeColor {
	light: UThemeColor,
	dark: UThemeColor,
}