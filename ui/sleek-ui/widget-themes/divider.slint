import { UAppTheme } from "../app-theme.slint";


export struct UDividerTheme {
	thickness: length,
	color: brush,
}


export global UDividerThemes {
    out property <UDividerTheme> default: {
        thickness: 1px * UAppTheme.scale-factor,
        color: UAppTheme.separator,
    };
}
