export enum UMenuItemType {
	link,
	submenu,
	group-title,
}


export struct UMenuData {
	id: string,
	name: string,
	type: UMenuItemType,
	children: [{
		id: string,
		name: string,
		type: UMenuItemType,
	}]
}