import { UAppTheme } from "../app-theme.slint";


export component USpinner inherits Image {
    in-out property <length> size: UAppTheme.icon-size-base;
    accessible-role: image;
    accessible-label: @tr("Loading spinner.");
    colorize: UAppTheme.primary.base;
    width: size;
    height: size;
    source: @image-url("../assets/icons/loader-circle.svg");
    transform-rotation: 240deg * animation-tick() / 1s;
	// Path {
    //             stroke: black;
    //             stroke-width: 4px;
    //             stroke-line-cap: round;
    //             private property <float> radius: slider.value;
    //             private property <float> start-x: self.viewbox-width / 2;
    //             private property <float> start-y: self.viewbox-height / 2;
    //             private property <float> start: 1 * mod(animation-tick(), 3s) / 2s;
    //             viewbox-width: 100;
    //             viewbox-height: 100;
    //             width: slider.value * 1px;
    //             height: slider.value * 1px;
    //             MoveTo {
    //                 x: start-x - radius * sin(-(parent.start - 0.001) * 360deg);
    //                 y: start-y - radius * cos(-(parent.start - 0.001) * 360deg);
    //             }

	// 			// Workaround for femtovg not filling complete circle
	// 			LineTo {
    //                 x: start-x - radius * sin(-parent.start * 360deg);
    //                 y: start-y - radius * cos(-parent.start * 360deg);
    //             }

    //             ArcTo {
    //                 private property <float> progress: 0.8;
    //                 radius-x: parent.radius;
    //                 radius-y: parent.radius;
    //                 x: start-x - parent.radius * sin(-(parent.start + progress) * 360deg);
    //                 y: start-y - parent.radius * cos(-(parent.start + progress) * 360deg);
    //                 sweep: true;
    //                 large-arc: progress > 0.5;
    //             }
    //         }
}
