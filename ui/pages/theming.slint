import { UAppTheme } from "../sleek-ui/app-theme.slint";
import { UTitle, UText, UCode, ULink } from "../sleek-ui/widgets.slint";

import { DocTitle } from "../components/doc-widgets.slint";
import { PageBase } from "../components/page-base.slint";
import { AppLogic } from "../app-logic.slint";


export component ThemingPage inherits PageBase {
    UTitle {
        level: 3;
        text: "Theming";
    }

	// TODO explain that the application's theme is automatically updated with Palette.color-scheme, but they can force a color scheme or change the Palette color scheme.
	// TODO explain how to change the application's theme: how the theme works, the UAppThemeConfigStruct (display it somewhere or link to github), make their own struct, set their struct in the UAppTheme global.
	// TODO also explain that all the properties are only out (except for both light and dark properties).
}
