import { UAppTheme } from "../../sleek-ui/app-theme.slint";
import {
    UTitle,
    UText,
} from "../../sleek-ui/widgets.slint";

import {
    DocTitle,
    DocDescription,
    WidgetPresentation,
    DocSection,
    DocCode,
} from "../../components/doc-widgets.slint";
import { PageBase } from "../../components/page-base.slint";
import { DocumentationLogic } from "../../logic/documentation-logic.slint";


export component MenuPageGroupTitle inherits PageBase {
    UTitle {
        level: 3;
        text: "Menu group title";
    }

    DocDescription {
        text: "A title to separate links and submenu from different groups in a menu.";
    }

    // TODO Present properties
}
