import { ScrollView } from "std-widgets.slint";
import { UAppTheme } from "../sleek-ui/app-theme.slint";

export component PageBase {
    height: 100%;
    ScrollView {
        height: 100%;
        VerticalLayout {
            alignment: start;
            padding: UAppTheme.padding-big;
            padding-top: UAppTheme.padding-bigger;
            @children
        }
    }
}
