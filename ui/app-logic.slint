export global AppLogic {
    out property <string> version: "v2.0.0";
    in-out property <float> scale-factor: 100;
    pure callback is-wasm() -> bool;
    pure callback update-scale-factor(scale-factor: float);
}
